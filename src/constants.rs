pub(crate) const SLEEP_DELAY: i32 = 50; // milliseconds
pub(crate) const REINIT_ATTEMPTS: u32 = 3; // maximum attempts to reinitialize the tunnel
pub(crate) const MAX_INIT_TUNNEL_ATTEMPTS: u32 = 3; // maximum attempts to send init_tunnel request
